# Pick one. Both free tiers work for the full MVP.
# Groq  -- published limits, no card:      https://console.groq.com/keys
GROQ_API_KEY=
# Gemini -- current-gen Flash, no card:    https://aistudio.google.com/apikey
GEMINI_API_KEY=
# Ollama needs no key; run `ollama serve` and use --provider ollama
